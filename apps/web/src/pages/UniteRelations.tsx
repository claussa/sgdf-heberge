import { PagePlaceholder } from './PagePlaceholder'

/** /unite/relations — écran A.16 « Demandes de mise en relation, » (page à venir). */
export function UniteRelations() {
  return <PagePlaceholder />
}
