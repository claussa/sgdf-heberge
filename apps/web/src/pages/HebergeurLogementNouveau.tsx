import { PagePlaceholder } from './PagePlaceholder'

/** /hebergeur/logements/nouveau — écrans A.10/A.11 création en 2 étapes (page à venir). */
export function HebergeurLogementNouveau() {
  return <PagePlaceholder />
}
