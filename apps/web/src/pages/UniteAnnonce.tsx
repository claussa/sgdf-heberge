import { PagePlaceholder } from './PagePlaceholder'

/**
 * /unite/annonce — écran A.13 « Notre annonce de jumelage, » (page à venir).
 * Reçoit éventuellement { kind: 'SEEKING' | 'HOSTING' } en state de navigation
 * depuis le profil unité.
 */
export function UniteAnnonce() {
  return <PagePlaceholder />
}
